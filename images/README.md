# Screenshot placeholders

These screenshots are intended for the public case study. Do not upload them until all personal, employer and account-specific information has been removed.

## 1. `zapier-workflow-overview.png`

Capture the full 9-step Zap in one view if possible.

Show:
- the Outlook trigger;
- the Formatter steps;
- the final Outlook calendar action;
- the 15-minute polling indicator if visible.

Hide or crop:
- the original Zap name if it contains the employer name;
- email addresses;
- account names;
- connection IDs;
- real class IDs;
- real message contents.

## 2. `zapier-timezone-conversion.png`

Open the timezone-conversion step and capture the configuration showing:
- source timezone `Europe/London`;
- destination timezone `Europe/Madrid`;
- the date/time transformation settings.

Hide or crop any live sample values, account information or production message content.

## 3. `zapier-calendar-action.png`

Open the final Outlook calendar step and capture enough of the configuration to show:
- Microsoft Outlook as the action app;
- calendar-event creation;
- mapped subject/start/end/body fields.

Hide or crop:
- connected Outlook account details;
- real calendar names if identifying;
- real class IDs;
- real dates/times from production data;
- email addresses or authentication information.

The screenshots should demonstrate the workflow design, not expose the original production environment.
